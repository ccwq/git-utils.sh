---
phase: quick-260414-l8m-sh-glob
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - sh/wsha.sh
  - docs/INSTALL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Shell wsha.sh loads config from config/wsh-alias/*.txt directories"
    - "Shell wsha.sh skips files starting with underscore (_)"
    - "Shell wsha.sh supports & and | prefix for alias definitions"
    - "Shell wsha.sh cache version updated to v3"
  artifacts:
    - path: "sh/wsha.sh"
      provides: "Directory glob loading, prefix parsing, cache v3"
      min_lines: 900
    - path: "docs/INSTALL.md"
      provides: "Updated documentation for directory config"
      contains: "wsh-alias/"
  key_links:
    - from: "sh/wsha.sh"
      to: "config/wsh-alias/"
      via: "load_config_dir glob"
      pattern: "load_config_dir.*\\*.txt"
    - from: "sh/wsha.sh"
      to: "parse_config_line"
      via: "_PARSED_PREFIX global"
      pattern: "parse_config_line.*prefix"
---

<objective>
修改 shell 版本 wsha.sh 以支持目录 glob 配置结构，与 Python 版本对齐。

Purpose: 让 Shell 版本支持与 Python 版本相同的目录 glob 配置结构（config/wsh-alias/*.txt），包括 & 和 | 前缀支持。
Output: 更新后的 sh/wsha.sh 和 docs/INSTALL.md
</objective>

<context>
@sh/wsha.sh
@py/wsha/parser.py (参考实现)
@py/wsha/config.py (参考实现)
@.planning/quick/260414-l8m-sh-glob/260414-l8m-RESEARCH.md

# 接口参考（从 py/wsha/parser.py 提取）

PREFIX_NORMAL = "normal"
PREFIX_SEQUENTIAL = "sequential"  # &foo
PREFIX_OR = "or"               # |foo

parse_dir(dir_path) -> (aliases, errors)
  - glob *.txt (1 level deep)
  - skip files starting with '_'
  - sort alphabetically

parse_line(line, config_path, line_no) -> (alias_name, template, prefix_type)
  - Check for & or | prefix at start
  - Return prefix_type as third element
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现目录 glob 加载和前缀解析</name>
  <files>sh/wsha.sh</files>
  <action>
## 修改 WSHA_CACHE_VERSION

在 ALIAS_TOKEN_SEP 旁边将 WSHA_CACHE_VERSION 从 'v2' 改为 'v3'。

## 新增 load_config_dir 函数

在 load_single_config_file 函数之前添加：

```bash
# 加载目录中的所有 *.txt 文件，跳过 _ 前缀文件，按字母序排序
load_config_dir() {
    local dir_path="$1"
    local fail_on_duplicate="$2"
    local source_name="$3"

    if [[ -z "$dir_path" ]] || [[ ! -d "$dir_path" ]]; then return 0; fi

    # glob 获取 *.txt 文件（1层深）
    local -a txt_files=()
    for f in "$dir_path"/*.txt; do
        [[ -f "$f" ]] && txt_files+=("$f")
    done

    # 按字母序排序（使用冒泡排序避免外部依赖）
    local sorted=false
    local n=${#txt_files[@]}
    while [[ "$sorted" == false ]]; do
        sorted=true
        for ((i=0; i<n-1; i++)); do
            if [[ "${txt_files[$i]}" > "${txt_files[$((i+1))]}" ]]; then
                local tmp="${txt_files[$i]}"
                txt_files[$i]="${txt_files[$((i+1))]}"
                txt_files[$((i+1))]="$tmp"
                sorted=false
            fi
        done
        ((n--))
    done

    local file
    for file in "${txt_files[@]}"; do
        local basename="${file##*/}"
        # 跳过 _ 前缀文件
        [[ "$basename" == _* ]] && continue
        load_single_config_file "$file" "$fail_on_duplicate" "$source_name" || return 1
    done
    return 0
}
```

## 新增全局变量

在 ALIAS_FIRST_TOKEN_LOWER 旁边添加：

```bash
declare -a ALIAS_PREFIX_TYPES=()
```

## 修改 parse_config_line 函数

在 _PARSED_TEMPLATE 返回之前，添加前缀检测逻辑：

```bash
# 检查 & 或 | 前缀（在 alias_name 解析之后）
_PARSED_PREFIX="normal"
if [[ "$_PARSED_ALIAS" == &* ]]; then
    _PARSED_PREFIX="sequential"
    _PARSED_ALIAS="${_PARSED_ALIAS:1}"
elif [[ "$_PARSED_ALIAS" == \|* ]]; then
    _PARSED_PREFIX="or"
    _PARSED_ALIAS="${_PARSED_ALIAS:1}"
fi
```

同时修改函数开头的局部变量声明添加 _PARSED_PREFIX。

## 修改 load_single_config_file 函数

在 build_alias_metadata 调用后，添加：

```bash
ALIAS_PREFIX_TYPES[$idx]="$_PARSED_PREFIX"
```

## 修改 load_config 函数

将 spec 解析格式从三段扩展为四段：

```bash
for spec in "${config_specs[@]}"; do
    IFS='|' read -r _spec_path _spec_dup _spec_source _spec_dir <<< "$spec"
    if [[ "$_spec_dir" == "dir" ]]; then
        load_config_dir "$_spec_path" "$_spec_dup" "$_spec_source" || return 1
    else
        load_single_config_file "$_spec_path" "$_spec_dup" "$_spec_source" || return 1
    fi
done
```

## 修改 main 函数中的配置路径

将单文件路径改为目录路径：

```bash
# 原来
local builtin_config="$APP_HOME/config/wsh-alias.txt"
local user_config="$HOME/.config/wsh-alias.txt"
local local_config="$(pwd)/.config/wsh-alias.txt"

# 改为目录
local builtin_config_dir="$APP_HOME/config/wsh-alias"
local user_config_dir="$HOME/.config/wsh-alias"
local local_config_dir="$(pwd)/.config/wsh-alias"
```

同时修改 load_config 调用：

```bash
if ! load_config "multi" \
    "$builtin_config_dir|false|内置|dir" \
    "$user_config_dir|false|用户级|dir" \
    "$local_config_dir|false|项目级|dir"; then exit 1; fi
```

## 修改 show_help 函数

更新配置路径和规则说明：

```bash
Config priority:
  1. config/wsh-alias/*.txt  (APP_HOME)
  2. $HOME/.config/wsh-alias/*.txt
  3. $PWD/.config/wsh-alias/*.txt

Rules:
  - Ignore empty lines and lines starting with '#'
  - Files starting with '_' are ignored
  - Same alias: first wins (loaded alphabetically)
  - Prefix & for sequential execution (stop on error)
  - Prefix | for or execution (stop on success)
```
</action>
  <verify>bash sh/wsha.sh --list 2>&1 | head -20</verify>
  <done>wsha.sh 能正常加载配置并显示 alias 列表，目录 glob 和前缀解析正常工作</done>
</task>

<task type="auto">
  <name>Task 2: 更新 INSTALL.md 文档</name>
  <files>docs/INSTALL.md</files>
  <action>
更新 docs/INSTALL.md 中与配置相关的部分：

1. 找到关于 WSHA_CONFIG_FILE 的说明，更新路径示例：
   - 将 `$HOME/.config/wsh-alias.txt` 改为 `$HOME/.config/wsh-alias/`
   - 将 `$PWD/.config/wsh-alias.txt` 改为 `$PWD/.config/wsh-alias/`

2. 在"常见问题"部分添加关于目录配置的说明：
   ```
   ### Q: 配置文件目录结构？

   wsha 支持多级配置文件目录：
   - 内置配置: `APP_HOME/config/wsh-alias/`
   - 用户级: `$HOME/.config/wsh-alias/`
   - 项目级: `$PWD/.config/wsh-alias/`

   每个目录下放置 `*.txt` 文件，文件名以 `_` 开头的会被忽略。
   ```

3. 保留其他内容不变，仅更新与配置路径相关的说明文字。
</action>
  <verify>grep -n "wsh-alias" docs/INSTALL.md | head -10</verify>
  <done>INSTALL.md 中所有 wsh-alias 路径都已更新为目录形式</done>
</task>

</tasks>

<verification>
# 快速验证检查

## 1. 语法检查
bash -n sh/wsha.sh && echo "Syntax OK"

## 2. 帮助信息验证
bash sh/wsha.sh --help 2>&1 | grep -A5 "Config priority"

## 3. 列表功能验证
bash sh/wsha.sh --list 2>&1 | head -20

## 4. 缓存版本验证
grep "WSHA_CACHE_VERSION" sh/wsha.sh
</verification>

<success_criteria>
- [ ] WSHA_CACHE_VERSION 更新为 'v3'
- [ ] load_config_dir 函数存在且正确实现
- [ ] ALIAS_PREFIX_TYPES 数组已声明
- [ ] parse_config_line 支持 & 和 | 前缀
- [ ] load_config 支持 dir 参数格式
- [ ] main 函数使用目录路径
- [ ] show_help 显示新的配置路径说明
- [ ] INSTALL.md 文档已更新
- [ ] bash -n 语法检查通过
</success_criteria>

<output>
After completion, create `.planning/quick/260414-l8m-sh-glob/260414-l8m-SUMMARY.md`
</output>
