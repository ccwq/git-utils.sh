---
phase: 04-cli-interface-shell-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - py/wsha/cli.py
  - pyproject.toml
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-04
  - SHELL-05

must_haves:
  truths:
    - "当 Python 内部抛出 ImportError / FileNotFoundError / RuntimeError 时，自动 fallback 执行 wsha.sh"
    - "非零退出码（如 w nonexistent 返回 127）不触发 fallback"
    - "uvx wsha --list 可以运行并显示别名列表"
    - "pip install wsha 后 w 命令通过 run_with_fallback 路由到 Python 实现"
  artifacts:
    - path: "py/wsha/cli.py"
      provides: "fallback_to_shell() + run_with_fallback() 实现"
      contains: "def fallback_to_shell"
      exports: ["run_with_fallback"]
    - path: "pyproject.toml"
      provides: "正确的包路径配置和 entry point"
      contains: "run_with_fallback"
  key_links:
    - from: "pyproject.toml [project.scripts] w"
      to: "wsha.cli:run_with_fallback"
      via: "pip install entry point"
    - from: "run_with_fallback()"
      to: "fallback_to_shell()"
      via: "except (ImportError, FileNotFoundError, RuntimeError)"
    - from: "fallback_to_shell()"
      to: "sh/wsha.sh"
      via: "subprocess.run(['bash', wsha_sh] + sys.argv[1:])"
    - from: "pyproject.toml [tool.hatch.build.targets.wheel] packages"
      to: "py/wsha package"
      via: "packages = [\"py/wsha\"] hatchling path"
---

<objective>
为 `py/wsha/cli.py` 实现 fallback 机制（D-25/26/27），并修正 `pyproject.toml` 的包路径配置，确保 `uvx wsha` 和 `pip install wsha` 安装后的 `w` 命令均正确路由。

Purpose: 完成 Python 实现的最后一公里——打包可发布、fallback 有保障，让用户在任何安装方式下都能使用 wsha。
Output: 带 fallback 的 entry point `run_with_fallback`，以及正确打包的 `pyproject.toml`。
</objective>

<execution_context>
@E:/project/self.project/git-utils.sh/.claude/get-shit-done/workflows/execute-plan.md
@E:/project/self.project/git-utils.sh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cli-interface-shell-integration/04-CONTEXT.md
@.planning/phases/04-cli-interface-shell-integration/04-01-SUMMARY.md

<interfaces>
<!-- 执行者需要的关键接口，从代码库提取。无需额外探索代码库。-->

From py/wsha/cli.py (after Plan 01):
```python
def main(...) -> None:
    """wsha - alias command launcher (Python implementation)"""
    # 含 --list / --list-view / --find / --cache-clear 处理逻辑
    # 含 别名展开逻辑（invoke_cmd，raise SystemExit(exit_code)）

# 文件位置：py/wsha/cli.py
# 包层级：py/ (project root) / wsha/ (package) / cli.py
# 因此 sh/wsha.sh 路径为：../../sh/wsha.sh (相对于 py/wsha/)
```

From pyproject.toml (current):
```toml
[project.scripts]
w = "wsha.cli:main"
wsha = "wsha.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["wsha"]   # 此行有误：wsha 包在 py/wsha/，需改为 ["py/wsha"]
```

Shell fallback target:
```
sh/wsha.sh  (位于项目根目录 sh/ 子目录下)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 fallback_to_shell() 和 run_with_fallback() 函数</name>
  <files>py/wsha/cli.py</files>
  <read_first>
    - py/wsha/cli.py (Plan 01 完成后的最新状态，避免覆盖已有改动)
    - sh/wsha.sh (确认文件存在于 sh/ 目录，验证路径解析逻辑)
    - .planning/phases/04-cli-interface-shell-integration/04-CONTEXT.md (D-25/26/27 约束)
  </read_first>
  <action>
在 py/wsha/cli.py 顶部的 import 区域增加（如不存在则添加）：
```python
import sys
import os
```

在 main() 函数定义之后（但在 `if __name__ == '__main__':` 之前）插入两个函数：

```python
def fallback_to_shell() -> int:
    """
    当 Python 实现失败时，调用 wsha.sh 作为 fallback。per D-27。

    路径解析：
      cli.py 位于 py/wsha/cli.py
      sh/wsha.sh 位于 project_root/sh/wsha.sh
      因此：os.path.dirname(__file__) = py/wsha/
             os.path.dirname(py/wsha/)  = py/
             os.path.dirname(py/)       = project_root/
    """
    pkg_dir = os.path.dirname(os.path.abspath(__file__))   # py/wsha/
    py_dir = os.path.dirname(pkg_dir)                       # py/
    project_dir = os.path.dirname(py_dir)                   # project root

    wsha_sh = os.path.join(project_dir, 'sh', 'wsha.sh')

    if not os.path.exists(wsha_sh):
        click.echo(
            f"[wsha] fallback failed: wsha.sh not found at {wsha_sh}",
            err=True
        )
        return 1

    # 直接调用 bash wsha.sh，透传 sys.argv[1:]（per D-27）
    # 使用 subprocess.run 而非 shell=True，避免二次 shell 注入
    result = subprocess.run(
        ['bash', wsha_sh] + sys.argv[1:],
        capture_output=False  # 透传 stdout/stderr 到调用方终端
    )
    return result.returncode


def run_with_fallback() -> None:
    """
    带 fallback 的 CLI 入口。供 pyproject.toml entry point 使用。

    per D-25: ImportError / FileNotFoundError / RuntimeError 触发 fallback
    per D-26: SystemExit（非零退出码）不触发 fallback——命令本身执行失败不是 Python 错误
    """
    try:
        main()
    except (ImportError, FileNotFoundError, RuntimeError) as exc:
        # D-25: Python 模块级错误触发 fallback
        sys.exit(fallback_to_shell())
    # SystemExit 不在此捕获，直接透传（D-26）
```

同时确保顶部已有 `import subprocess`（main() 函数体已有使用，若无则在文件顶部添加）。

将文件末尾的 `if __name__ == '__main__':` 块更新为调用 `run_with_fallback()`：
```python
if __name__ == '__main__':
    run_with_fallback()
```
  </action>
  <verify>
    <automated>cd "E:/project/self.project/git-utils.sh" && python -c "
import sys; sys.path.insert(0, 'py')
from wsha.cli import fallback_to_shell, run_with_fallback, main
print('fallback_to_shell import: OK')
print('run_with_fallback import: OK')

# 验证 fallback_to_shell 可以找到 wsha.sh
import os
pkg_dir = os.path.dirname(os.path.abspath('py/wsha/cli.py'))
# 验证函数存在且可调用
import inspect
sig = inspect.signature(fallback_to_shell)
print('fallback_to_shell signature:', sig)
print('Task 1: PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - py/wsha/cli.py 包含 `def fallback_to_shell(`
    - py/wsha/cli.py 包含 `def run_with_fallback(`
    - py/wsha/cli.py 包含 `except (ImportError, FileNotFoundError, RuntimeError)`
    - py/wsha/cli.py 不包含 `except SystemExit`（SystemExit 不应被 fallback 捕获）
    - py/wsha/cli.py 末尾 `if __name__ == '__main__':` 块调用 `run_with_fallback()`
    - `from wsha.cli import fallback_to_shell, run_with_fallback` 返回 0
  </acceptance_criteria>
  <done>fallback_to_shell() 通过 os.path.dirname 三次向上解析定位 sh/wsha.sh，不存在时输出 stderr 错误并返回 1；run_with_fallback() 捕获 ImportError/FileNotFoundError/RuntimeError 后调用 fallback，SystemExit 透传不捕获；__main__ 块调用 run_with_fallback。</done>
</task>

<task type="auto">
  <name>Task 2: 修正 pyproject.toml 包路径，更新 entry point 指向 run_with_fallback</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (当前完整内容，确保完整保留 [build-system] 和 [project] 字段)
    - py/wsha/__init__.py (确认包名为 wsha，确认 VERSION 和导出)
  </read_first>
  <action>
将 pyproject.toml 完整替换为以下内容（保留所有现有字段，仅修改 packages 路径和 entry point 目标）：

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wsha"
version = "0.1.0"
description = "wsha - alias command launcher"
requires-python = ">=3.8"
dependencies = [
    "click>=8.0",
]

[project.scripts]
# SHELL-01: uvx wsha 使用此 entry point
# SHELL-02: pip install wsha 后 w 命令使用此 entry point
# SHELL-05: w <alias> 路由到 Python 实现（通过 run_with_fallback）
# SHELL-04: run_with_fallback 包含 fallback 逻辑
w = "wsha.cli:run_with_fallback"
wsha = "wsha.cli:run_with_fallback"

[tool.hatch.build.targets.wheel]
# packages = ["py/wsha"] 告诉 hatchling：py/wsha/ 目录作为 wsha 包打包到 wheel
# 修正前为 ["wsha"]，该路径在项目根目录下不存在
packages = ["py/wsha"]
```

注意：不要更改任何其他字段。仅修改：
1. `[project.scripts]` 中两个 entry point 从 `wsha.cli:main` → `wsha.cli:run_with_fallback`
2. `[tool.hatch.build.targets.wheel]` 中 `packages = ["wsha"]` → `packages = ["py/wsha"]`
  </action>
  <verify>
    <automated>cd "E:/project/self.project/git-utils.sh" && python -c "
import tomllib, pathlib
data = tomllib.loads(pathlib.Path('pyproject.toml').read_text())
scripts = data['project']['scripts']
assert scripts['w'] == 'wsha.cli:run_with_fallback', f'w entry point wrong: {scripts[\"w\"]}'
assert scripts['wsha'] == 'wsha.cli:run_with_fallback', f'wsha entry point wrong: {scripts[\"wsha\"]}'
packages = data['tool']['hatch']['build']['targets']['wheel']['packages']
assert 'py/wsha' in packages, f'packages path wrong: {packages}'
print('pyproject.toml validation: PASS')
print('  w =>', scripts['w'])
print('  wsha =>', scripts['wsha'])
print('  packages =>', packages)
" 2>/dev/null || python -c "
# Python < 3.11 fallback: use configparser-style check
import re, pathlib
content = pathlib.Path('pyproject.toml').read_text()
assert 'run_with_fallback' in content, 'run_with_fallback not in pyproject.toml'
assert 'py/wsha' in content, 'py/wsha not in pyproject.toml'
print('pyproject.toml content check: PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml 包含 `w = "wsha.cli:run_with_fallback"`
    - pyproject.toml 包含 `wsha = "wsha.cli:run_with_fallback"`
    - pyproject.toml 包含 `packages = ["py/wsha"]`
    - pyproject.toml 不包含 `packages = ["wsha"]`（旧的错误路径已删除）
    - pyproject.toml 不包含 `wsha.cli:main`（旧的 entry point 已全部替换）
    - `pip install -e . --dry-run` 或 `python -m build --wheel --no-isolation` 可以找到 py/wsha 包（可选验证，环境允许时执行）
  </acceptance_criteria>
  <done>pyproject.toml [project.scripts] 的 w 和 wsha 均指向 wsha.cli:run_with_fallback；[tool.hatch.build.targets.wheel] packages 为 ["py/wsha"]，hatchling 可正确将 py/wsha/ 打包为 wsha 包。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sys.argv → subprocess args | sys.argv[1:] 原样传递给 wsha.sh，不经过 shell 解释 |
| Python process → bash subprocess | fallback 调用 bash wsha.sh，路径从文件系统解析 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | fallback_to_shell / subprocess args | mitigate | 使用 `subprocess.run(['bash', wsha_sh] + sys.argv[1:])` 而非 `shell=True`，避免 shell 注入；参数列表拼接，不经过 shell 展开 |
| T-04-05 | Elevation of Privilege | fallback infinite loop | mitigate | fallback 直接调用 `sh/wsha.sh`，不通过 `w` 或 `wsha` entry point，切断递归路径（per pitfall 4）|
| T-04-06 | Denial of Service | fallback path resolution failure | accept | wsha.sh 不存在时输出 stderr 错误并返回 1，不 crash；用户需重新安装 |
</threat_model>

<verification>
完成后运行完整验证：

```bash
# 1. 验证 pyproject.toml 内容正确
cd E:/project/self.project/git-utils.sh
grep -n "run_with_fallback" pyproject.toml
grep -n "py/wsha" pyproject.toml

# 2. 验证 cli.py 函数存在且可导入
python -c "
import sys; sys.path.insert(0, 'py')
from wsha.cli import main, fallback_to_shell, run_with_fallback
print('All functions importable: OK')
"

# 3. 验证 fallback 不捕获 SystemExit（非零退出不触发 fallback）
python -c "
import sys; sys.path.insert(0, 'py')
from click.testing import CliRunner
from wsha.cli import main
runner = CliRunner()
# 已知不存在的别名会 passthrough 并返回非零，这不应触发 fallback
result = runner.invoke(main, ['__nonexistent_alias_xyz__'])
print('Non-zero exit test - exit_code:', result.exit_code, '(should NOT be fallback behavior)')
"
```
</verification>

<success_criteria>
1. `from wsha.cli import run_with_fallback` 不报错
2. pyproject.toml `[project.scripts]` 中 `w` 和 `wsha` 均指向 `wsha.cli:run_with_fallback`
3. pyproject.toml `packages = ["py/wsha"]` — hatchling 可以找到包
4. fallback_to_shell() 中不存在 shell=True 参数（subprocess 安全）
5. run_with_fallback() 中不捕获 SystemExit（非零退出码透传，per D-26）
6. fallback 路径解析使用 os.path.dirname 从 __file__ 三级向上，不硬编码绝对路径
</success_criteria>

<output>
完成后创建 `.planning/phases/04-cli-interface-shell-integration/04-02-SUMMARY.md`：

```markdown
## Plan 04-02 Summary

**Completed:** [date]
**Phase:** 04-cli-interface-shell-integration

### What Was Built
- Added fallback_to_shell() to py/wsha/cli.py: resolves sh/wsha.sh via __file__ path, invokes bash subprocess
- Added run_with_fallback() to py/wsha/cli.py: catches ImportError/FileNotFoundError/RuntimeError, does NOT catch SystemExit
- Fixed pyproject.toml: packages = ["py/wsha"] (was ["wsha"]), entry points → run_with_fallback

### Key Decisions
- D-25: Only ImportError/FileNotFoundError/RuntimeError trigger fallback
- D-26: SystemExit (non-zero exit from command) does NOT trigger fallback
- D-27: Fallback invokes sh/wsha.sh directly via subprocess, not through w entry point (avoids loop)
- D-33/34: uvx wsha and pip install w both work via run_with_fallback entry point

### Files Modified
- py/wsha/cli.py — added fallback_to_shell(), run_with_fallback(), updated __main__
- pyproject.toml — fixed packages path, updated entry points

### Requirements Met
- SHELL-01, SHELL-02, SHELL-04, SHELL-05
```
</output>
