---
phase: 04-cli-interface-shell-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - py/wsha/cli.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04

must_haves:
  truths:
    - "w --list 以表格格式显示所有别名，按来源分组（builtin/user/project）"
    - "w --list-view 显示完整别名元数据：名称、模板、来源配置文件、行号"
    - "w --find <pattern> 使用 fnmatch glob 匹配别名名称并返回结果"
    - "w --cache-clear 清除缓存并打印 'Cache cleared.'"
  artifacts:
    - path: "py/wsha/cli.py"
      provides: "Enhanced CLI with --list/-l, --list-view/-lv, --find/-f, --cache-clear options"
      contains: "def show_list_table"
      exports: ["main", "show_list_table", "show_list_view", "find_aliases"]
  key_links:
    - from: "main() --list option"
      to: "show_list_table()"
      via: "if list_mode: show_list_table(aliases, sources)"
    - from: "main() --list-view option"
      to: "show_list_view()"
      via: "if list_view_mode: show_list_view(aliases, sources)"
    - from: "main() --find option"
      to: "find_aliases()"
      via: "if find_pattern is not None: find_aliases(aliases, find_pattern)"
---

<objective>
为 `py/wsha/cli.py` 增加 `--list`/`-l`、`--list-view`/`-lv`、`--find`/`-f`、`--cache-clear` 四个 CLI 选项，实现别名列表显示、详细视图和 glob 搜索功能。

Purpose: 让用户可以发现和搜索已配置的别名，提升 wsha 可用性。
Output: 增强后的 `py/wsha/cli.py`，支持完整 CLI 选项集（CLI-01~CLI-04）。
</objective>

<execution_context>
@E:/project/self.project/git-utils.sh/.claude/get-shit-done/workflows/execute-plan.md
@E:/project/self.project/git-utils.sh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cli-interface-shell-integration/04-CONTEXT.md

<interfaces>
<!-- 执行者需要的关键接口，从代码库提取。无需额外探索代码库。-->

From py/wsha/config.py:
```python
class AliasEntry:
    name: str          # 别名名称（如 "ab", "px*"）
    template: str      # 命令模板（如 "pnpx agent-browser"）
    config_path: str   # 来源配置文件绝对路径
    source_name: str   # 来源标签："builtin" | "user" | "project"
    line_no: int       # 在配置文件中的行号

def load_config(
    mode: str = "multi",
    config_path: Optional[str] = None,
    use_cache: bool = True
) -> Tuple[List[AliasEntry], List[ConfigParseError], Dict[str, str]]:
    """
    返回 (aliases, errors, sources)
    - aliases: AliasEntry 列表
    - errors: 解析错误列表
    - sources: Dict[source_name, config_path]，如 {"builtin": "/path/to/config/wsh-alias.txt"}
    """
```

From py/wsha/cache.py:
```python
class CacheManager:
    def clear(self) -> None:
        """删除 ~/.cache/wsha/ 下所有 *.json 缓存文件"""
```

Current py/wsha/cli.py structure (to be modified):
```python
@click.command()
@click.argument('alias_input', required=False)
@click.argument('args', nargs=-1, type=click.UNPROCESSED)
@click.option('--help', '-h', is_flag=True, help='Show help')
def main(alias_input: str, args: Tuple[str, ...], help: bool) -> None:
    """wsha - alias command launcher (Python implementation)"""
    # ... alias expansion logic already implemented
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 show_list_table、show_list_view、find_aliases 辅助函数</name>
  <files>py/wsha/cli.py</files>
  <read_first>
    - py/wsha/cli.py (当前完整实现，避免覆盖已有导入和 main() 逻辑)
    - py/wsha/config.py (AliasEntry 字段确认：name, template, config_path, source_name, line_no)
    - sh/wsha.sh L562-613 (show_list_table shell 参考实现，Python 版本需对齐行为)
  </read_first>
  <behavior>
    - show_list_table([], {}) 输出 "[wsha] no alias found."
    - show_list_table([entry], {"builtin": "/path"}) 输出含 "[builtin]" 标题的表格
    - 超过 60 字符的 template 截断为 57 字符 + "..."
    - show_list_view([], {}) 输出 "[wsha] no alias found."
    - show_list_view([entry], {"builtin": "/path"}) 每条别名显示 Template/Source/Config:line_no
    - find_aliases(aliases, "ab*") 返回 name 匹配 fnmatch("ab*") 的别名列表
    - find_aliases(aliases, "xyz") 当无匹配时返回空列表
  </behavior>
  <action>
在 py/wsha/cli.py 顶部的 import 区域增加如下导入（如不存在则添加）：
```python
import fnmatch
from typing import Tuple, List, Optional, Dict
```

在 main() 函数定义之前，插入三个辅助函数：

```python
def show_list_table(aliases: List[AliasEntry], sources: Dict[str, str]) -> None:
    """以表格格式按来源分组显示别名列表。per D-24。"""
    found_any = False
    for source_name, config_path in sources.items():
        # 过滤属于该来源的条目（按 source_name 匹配）
        group = [a for a in aliases if a.source_name == source_name]
        if not group:
            continue
        found_any = True
        max_alias_len = max((len(a.name) for a in group), default=4)
        max_alias_len = max(max_alias_len, 4)  # 最小宽度 4

        # 来源标题
        click.echo(f"[{source_name}] {config_path}")
        click.echo("")
        # 表头
        click.echo(f"{'别名':<{max_alias_len}}  命令")
        click.echo(f"{'----':<{max_alias_len}}  ----")
        # 数据行
        for entry in group:
            template = entry.template if len(entry.template) <= 60 else entry.template[:57] + "..."
            click.echo(f"{entry.name:<{max_alias_len}}  {template}")
        click.echo("")
    if not found_any:
        click.echo("[wsha] no alias found.")


def show_list_view(aliases: List[AliasEntry], sources: Dict[str, str]) -> None:
    """显示完整别名元数据：名称/模板/来源配置文件/行号。per D-29。"""
    found_any = False
    for source_name, config_path in sources.items():
        group = [a for a in aliases if a.source_name == source_name]
        if not group:
            continue
        found_any = True
        click.echo(f"[{source_name}] {config_path}")
        click.echo("")
        for entry in group:
            click.echo(f"  {entry.name}")
            click.echo(f"    Template: {entry.template}")
            click.echo(f"    Source:   {entry.source_name}")
            click.echo(f"    Config:   {entry.config_path}:{entry.line_no}")
            click.echo("")
    if not found_any:
        click.echo("[wsha] no alias found.")


def find_aliases(aliases: List[AliasEntry], pattern: str) -> List[AliasEntry]:
    """使用 fnmatch glob 模式搜索别名。per D-28。不使用 regex。"""
    return [a for a in aliases if fnmatch.fnmatch(a.name, pattern)]
```
  </action>
  <verify>
    <automated>cd "E:/project/self.project/git-utils.sh" && python -c "
import sys; sys.path.insert(0, 'py')
from wsha.cli import show_list_table, show_list_view, find_aliases
from wsha.config import AliasEntry
# 基本导入验证
e = AliasEntry('ab', 'pnpx agent-browser', '/cfg', 'builtin', 1)
results = find_aliases([e], 'a*')
assert results == [e], f'find_aliases failed: {results}'
results = find_aliases([e], 'xy*')
assert results == [], f'find_aliases should return empty: {results}'
print('Task 1: PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - py/wsha/cli.py 包含 `def show_list_table(`
    - py/wsha/cli.py 包含 `def show_list_view(`
    - py/wsha/cli.py 包含 `def find_aliases(`
    - py/wsha/cli.py 包含 `import fnmatch`
    - python -c "from wsha.cli import show_list_table, show_list_view, find_aliases" 返回 0（路径设置 sys.path 包含 py/）
  </acceptance_criteria>
  <done>三个辅助函数存在于 cli.py 中，find_aliases 正确使用 fnmatch，show_list_table 按 source_name 分组，show_list_view 输出四行元数据（名称/Template/Source/Config:lineno）。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 main() Click 选项并串联所有处理逻辑</name>
  <files>py/wsha/cli.py</files>
  <read_first>
    - py/wsha/cli.py (Task 1 完成后的状态，确认辅助函数已存在)
  </read_first>
  <action>
将 main() 函数的 @click.command() 装饰器替换为带全部新选项的版本，并更新函数签名和函数体：

装饰器（替换现有 @click.command() 块）：
```python
@click.command(context_settings=dict(help_option_names=['-h', '--help']))
@click.argument('alias_input', required=False)
@click.argument('args', nargs=-1, type=click.UNPROCESSED)
@click.option('--list', '-l', 'list_mode', is_flag=True, help='List all aliases in table format')          # CLI-01, D-24
@click.option('--list-view', '-lv', 'list_view_mode', is_flag=True, help='Show detailed alias view')       # CLI-02, D-29
@click.option('--find', '-f', 'find_pattern', default=None, metavar='PATTERN', help='Search aliases by glob pattern')  # CLI-03, D-28
@click.option('--cache-clear', is_flag=True, help='Clear config cache')                                    # CLI-04
```

函数签名（替换现有 def main(...)）：
```python
def main(
    alias_input: str,
    args: Tuple[str, ...],
    list_mode: bool,
    list_view_mode: bool,
    find_pattern: Optional[str],
    cache_clear: bool,
) -> None:
    """wsha - alias command launcher (Python implementation)"""
```

函数体的新选项处理逻辑，插入在 `if help or not alias_input:` 判断之前（保留现有别名展开逻辑）：

```python
    # CLI-04: --cache-clear 立即执行后退出，不加载 config
    if cache_clear:
        CacheManager().clear()
        click.echo("Cache cleared.")
        return

    # 加载配置（--list / --list-view / --find 需要 aliases 数据）
    if list_mode or list_view_mode or find_pattern is not None:
        aliases, errors, sources = load_config()
        if errors:
            for err in errors:
                click.echo(f"[wsha] config error: {err}", err=True)

        # CLI-01: --list / -l  per D-24
        if list_mode:
            show_list_table(aliases, sources)
            return

        # CLI-02: --list-view / -lv  per D-29
        if list_view_mode:
            show_list_view(aliases, sources)
            return

        # CLI-03: --find  per D-28
        if find_pattern is not None:
            matches = find_aliases(aliases, find_pattern)
            if not matches:
                click.echo(f"[wsha] no alias matching '{find_pattern}'")
            else:
                for entry in matches:
                    max_len = max(len(e.name) for e in matches)
                    click.echo(f"{entry.name:<{max_len}}  {entry.template}")
            return

    # 无参数或 --help：显示帮助
    if not alias_input:
        click.echo("wsha - alias command launcher")
        click.echo("Usage: w <alias> [args...]")
        click.echo("       w --list | -l")
        click.echo("       w --list-view | -lv")
        click.echo("       w --find <pattern>")
        click.echo("       w --cache-clear")
        return
```

保留 main() 函数体其余的别名展开逻辑（从 `# Build full input text` 开始的部分）不修改。

同时更新顶部导入，添加 CacheManager：
```python
from .cache import CacheManager
```
  </action>
  <verify>
    <automated>cd "E:/project/self.project/git-utils.sh" && python -c "
import sys; sys.path.insert(0, 'py')
from click.testing import CliRunner
from wsha.cli import main
runner = CliRunner()
# 验证 --list 选项存在
result = runner.invoke(main, ['--help'])
assert '--list' in result.output, f'--list missing from help: {result.output}'
assert '--list-view' in result.output, f'--list-view missing from help: {result.output}'
assert '--find' in result.output, f'--find missing from help: {result.output}'
assert '--cache-clear' in result.output, f'--cache-clear missing from help: {result.output}'
print('Task 2 help check: PASS')
# 验证 --cache-clear 运行不报错
result = runner.invoke(main, ['--cache-clear'])
assert result.exit_code == 0, f'--cache-clear failed: {result.output}'
assert 'Cache cleared' in result.output, f'Cache cleared message missing: {result.output}'
print('Task 2 cache-clear check: PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - py/wsha/cli.py 中 @click.command() 装饰器包含 `'--list', '-l', 'list_mode'`
    - py/wsha/cli.py 中 @click.command() 装饰器包含 `'--list-view', '-lv', 'list_view_mode'`
    - py/wsha/cli.py 中 @click.command() 装饰器包含 `'--find', '-f', 'find_pattern'`
    - py/wsha/cli.py 中 @click.command() 装饰器包含 `'--cache-clear'`
    - `python -m click.testing` + `runner.invoke(main, ['--help'])` 输出包含 "--list", "--list-view", "--find", "--cache-clear"
    - `runner.invoke(main, ['--cache-clear'])` 退出码 0，输出包含 "Cache cleared"
  </acceptance_criteria>
  <done>main() 接收 list_mode/list_view_mode/find_pattern/cache_clear 四个参数，--cache-clear 清除缓存后退出，--list 调用 show_list_table，--list-view 调用 show_list_view，--find 调用 find_aliases 并输出结果。原有别名展开逻辑保持不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → --find pattern | 用户提供 fnmatch 模式作为 CLI 参数 |
| config file → AliasEntry | 解析 wsh-alias.txt 中的别名名称和模板字符串 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | find_aliases / fnmatch | accept | fnmatch 不执行代码，仅字符串匹配，无 eval 风险 |
| T-04-02 | Denial of Service | find_aliases / fnmatch | accept | 模式由用户自己提供，无服务端影响，拒绝服务仅影响自身进程 |
| T-04-03 | Information Disclosure | show_list_view | accept | 仅显示本地配置文件路径（本机用户已有访问权限） |
</threat_model>

<verification>
完成后运行完整验证：

```bash
cd E:/project/self.project/git-utils.sh
python -c "
import sys; sys.path.insert(0, 'py')
from click.testing import CliRunner
from wsha.cli import main
runner = CliRunner()

# CLI-01: --list
r = runner.invoke(main, ['--list'])
print('--list exit:', r.exit_code, '| output length:', len(r.output))

# CLI-02: --list-view
r = runner.invoke(main, ['--list-view'])
print('--list-view exit:', r.exit_code, '| output length:', len(r.output))

# CLI-03: --find
r = runner.invoke(main, ['--find', 'a*'])
print('--find exit:', r.exit_code, '| output:', r.output.strip())

# CLI-04: --cache-clear
r = runner.invoke(main, ['--cache-clear'])
assert r.exit_code == 0 and 'Cache cleared' in r.output
print('--cache-clear: PASS')
"
```
</verification>

<success_criteria>
1. `w --list` 或 `w -l`：输出含 "[builtin]"/"[user]"/"[project]" 分组表格，列：别名名称、命令模板（超 60 字符截断）
2. `w --list-view` 或 `w -lv`：每条别名输出 Template/Source/Config:lineno 四行元数据
3. `w --find "ab*"`：返回 name 匹配 fnmatch("ab*") 的别名行，无匹配时输出提示
4. `w --cache-clear`：输出 "Cache cleared." 后退出 0
5. 原有 `w <alias> [args...]` 别名展开逻辑未被破坏
</success_criteria>

<output>
完成后创建 `.planning/phases/04-cli-interface-shell-integration/04-01-SUMMARY.md`：

```markdown
## Plan 04-01 Summary

**Completed:** [date]
**Phase:** 04-cli-interface-shell-integration

### What Was Built
- Added show_list_table(), show_list_view(), find_aliases() to py/wsha/cli.py
- Enhanced @click.command() with --list/-l, --list-view/-lv, --find/-f, --cache-clear options
- Wired all option handlers in main()

### Key Decisions
- D-24: Table format columns = name/template (truncated at 60 chars), grouped by source_name
- D-28: fnmatch.fnmatch() for --find, not regex
- D-29: list-view shows name/Template/Source/Config:lineno per alias

### Files Modified
- py/wsha/cli.py — added 3 helper functions, 4 new Click options

### Requirements Met
- CLI-01, CLI-02, CLI-03, CLI-04
```
</output>
