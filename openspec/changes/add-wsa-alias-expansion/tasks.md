## 1. 配置与入口准备

- [x] 1.1 新增 `config/wsh-alias.txt` 示例内容，覆盖 `ab`、`foo`、`bar` 三类映射格式
- [x] 1.2 新增 `sh/wsha.bat` 基础入口，支持 `wsa <alias> [args...]` 调用与参数接收
- [x] 1.3 在 `wsha.bat` 中实现相对脚本路径定位配置文件，避免依赖当前工作目录

## 2. 配置解析能力

- [x] 2.1 实现逐行读取配置并忽略空行与 `#` 注释行
- [x] 2.2 实现 `<alias> <target...>` 分词解析并构建 alias 映射表
- [x] 2.3 增加配置校验：空 alias、缺失 target、重复 alias 时输出错误并非零退出

## 3. 命令展开与执行

- [x] 3.1 实现已知 alias 的模板命令展开并执行
- [x] 3.2 实现参数合并规则：无 `--` 时追加运行时参数到末尾
- [x] 3.3 实现占位插入规则：有 `--` 时在占位位置插入运行时参数
- [x] 3.4 实现未知 alias、缺少 alias 参数等错误路径的提示与非零退出码

## 4. 测试与文档同步

- [x] 4.1 新增测试用例覆盖 `ab`、`foo`、`bar`、未知 alias、重复 alias、非法配置等场景
- [x] 4.2 在测试全部通过后更新相关文档，补充配置格式与使用示例
